<% @categories.each do |category| %>
<div class="container mb-4 mt-4 col-2">
  <div class="card text-center">

    <div class="card-body">
      <h5 class="card-title"> <%= link_to category.name, category %> </h5>
            <p class="card-text text-muted"> x articles in this category </p>

    </div>
  </div>
</div>
  
<% end %>