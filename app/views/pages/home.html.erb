<div class="container text-center" id="home-container" >
  <div class="jumbotron">
    <h1 class="display-4">Alpha Blog</h1>
    <p class="lead">A Ruby on Rails project!</p>
    <hr class="my-4">
    <p>Start writing now!</p>
    <% if logged_in? %>
      <a class="btn btn-primary btn-lg" href=<%= new_article_path %> role="button">Write</a>
    <% else %>
      <a class="btn btn-primary btn-lg" href=<%= signup_path %> role="button">Sign Up</a>
    <% end %>
  </div>
</div>